# AI BOOT

Before touching the repository:

Read every file inside the .ai directory.

These files are your permanent memory.

Do not perform another repository-wide scan unless a required module cannot be found.

After reading .ai:

Follow TASK_QUEUE.md.

After each completed task:

Update PROJECT_STATE.md.

Continue automatically.

Never restart the project analysis.